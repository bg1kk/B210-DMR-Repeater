<!--
Compiled and written by BG1KK.
Privatization and closed-source use are strictly forbidden.
GNU Radio components are copyrighted by their respective developers.
All other code copyright © BG1KK.
This copyright statement must be retained.
-->
# B210 内部三档 RSSI 校准实施方案

## 1. 适用结论

本方案只使用 B210 内部 AD9361、UHD 和软件原始 IQ 测量，不使用外部射频量程切换器。天线口工程输入上限规定为 `-20 dBm`，低于 B210 官方约 `-15 dBm` 接收上限并保留 5 dB 余量。`-20 dBm` 以上的近距离大功率发射必须在天线端外接衰减，不能由软件 AGC或数字 AGC解决。

校准和正常运行都关闭 B210 硬件 AGC。接收增益固定为三档：`low=0 dB`、`medium=25 dB`、`high=50 dB` 为首次测试候选值。软件数字 AGC位于 RSSI/SNR测量点之后，只调节解调输入，不参与信号强度计量。

目标有效范围为 `-125 .. -20 dBm`。`-125 dBm` 校准点的 DMR匹配滤波后 SNR 必须不低于 12 dB；SNR测量必须使用与正常运行相同的滤波器、采样率和实时积分时间，记录等效噪声带宽，禁止通过校准时额外缩窄带宽或非实时长积分制造合格值。这不是理论承诺，必须由每个物理 RX、工作频率和带宽分别实测确认。

`-125 dBm` 加 12 dB SNR要求等效噪声不高于 `-137 dBm`。290 K 下完整12.5 kHz带宽的热噪声约为 `-133 dBm`，还未计入B210噪声系数，因此完整通道原始SNR不可能达到12 dB。验收必须同时记录原始通道 `channel_snr_db` 与正常DMR匹配滤波后的 `demod_snr_db`；12 dB门限适用于后者。提高B210 Gain只改善ADC量化利用率，不能突破天线口热噪声限制。

## 2. 三档覆盖设计

| 档位 | 固定 Gain 候选值 | 校准点 | 设计用途 |
|---|---:|---|---|
| high | 50 dB | -125,-120,-115,-110,-105,-100,-95,-90,-85,-80,-75 | 弱信号，SNR >=12 dB |
| medium | 25 dB | -85,-80,-75,-70,-65,-60,-55,-50,-45 | 中等信号 |
| low | 0 dB | -55,-50,-45,-40,-35,-30,-25,-20 | 强信号 |

相邻档位均有 10 dB 重叠。自动模式使用原始 dBFS和削顶状态切换档位，不使用已经换算出的 dBm 反过来控制增益。启动或故障时选择 low；原始 IQ 1 ms 窗口出现削顶、削顶比例超限或 RMS 达到 `-8 dBFS` 时立即降到 low。上方安全区连续 20 ms 降档，下方安全区连续 500 ms 升档；切档后 20 ms 数据无效并重置软件数字 AGC。

## 3. 实验室设备

推荐链路：

```text
校准信号源 0 dBm -> 20 dB 固定衰减器 -> 0..90 dB 步进衰减器
-> 50 ohm 屏蔽同轴 -> B210 RX1 或 RX2
```

信号源不确定度不超过 0.5 dB，衰减链路合成不确定度不超过 0.7 dB，步进不大于 2.5 dB。用功率计在 B210 参考面复核 `-20 dBm`，总衰减约 110 dB 时检查线缆泄漏和接收底噪。B210、信号源、衰减器预热至少 30 分钟。校准附件在正式运行时移除。

## 4. 单点校准流程

1. 停止转发并确认 TX/PTT 已释放；锁定 RX、频率、带宽、天线和采样率。
2. 关闭硬件 AGC，设置当前固定 Gain并回读；测量无输入噪声底至少 2 秒。
3. 从该档最弱点开始，按 5 dB 递增到最强点，严禁强到弱倒序。
4. 每个输入点等待至少 1 秒，再采集不少于 10 个 200 ms 窗口，覆盖至少 2 秒。
5. 仅当峰峰值 <=0.8 dB、标准差 <=0.3 dB、medium/low 的 `demod_snr_db` >=10 dB、high 的 `demod_snr_db` >=12 dB、无 UHD错误、RMS <=-8 dBFS、削顶比例 <0.001 时提交；同时保存 `channel_snr_db` 和 `snr_enbw_hz`。
6. 在功率线性域执行 `Psignal=max(Ptotal-Pnoise,Pfloor)`，再转为 dBFS；去除最高和最低窗口后平均剩余窗口的线性功率。
7. 记录输入 dBm、dBFS、SNR、噪声底、实际 Gain、温度、削顶计数和时间戳。

## 5. 拟合和运行时计算

每个 RX/频率/带宽/档位保存独立节点，使用 `measured_dbfs -> input_dbm` 单调分段线性拟合。禁止全局高阶多项式、端点外推和“当前 Gain 与参考 Gain 相减”的补偿公式。配置使用临时文件、重新解析校验和原子替换；旧的两档数据备份为 `.legacy` 并标记 `needs_recalibration`，不得静默迁移。

运行时只有在当前物理 RX、固定档位和曲线完整且 dBFS 位于实测范围内时输出 dBm。切档 settling、削顶、UHD overflow、曲线缺失和范围外输入均输出 `rssi_dbm=null`。low 档仍削顶或估计输入达到 `-20 dBm` 时置 `rf_overrange=true`。

## 6. 定量验收

使用未参与拟合的 2.5 dB 验证网格。`-105 .. -30 dBm` 区间最大绝对误差 <=2.5 dB、RMS <=1.5 dB；`-125 .. -105 dBm` 和 `-30 .. -20 dBm` 端点区间最大绝对误差 <=3 dB、RMS <=2 dB；`-125 dBm` 点 SNR >=12 dB；重叠区跨档不连续 <=2 dB，曲线严格单调。RX1和RX2均需完成。

## 7. 两档与三档比较

两档可以作为实验选项，但不能直接作为发布默认值。删除 medium 后，high 档需要从约 `-125 dBm` 一直覆盖到 `-55 dBm`，单档动态范围约 70 dB；它必须同时满足弱端 SNR >=12 dB和强端无削顶。12 bit ADC、接收机噪声系数、滤波噪声和增益级非线性会显著压缩可用余量。

| 项目 | 三档 0/25/50 dB | 两档 0/50 dB |
|---|---|---|
| 总目标范围 | -125..-20 dBm | 理论相同，需额外证明 |
| 单档最大跨度 | 约 50 dB | high 约 70 dB |
| 重叠与切换余量 | 两个约 10 dB 重叠区 | 只有一个重叠区，余量小 |
| 突发强信号风险 | medium 可提前降档 | high 到 low 跨度大，较易瞬时削顶 |
| 校准和GUI复杂度 | 11+9+8 点/每RX | 最少两列，但 high 必须增加到 -55 dBm |
| 推荐结论 | 发布默认方案 | 仅在全范围实测通过后启用 |

两档验收必须在 high 固定 Gain下增加 `-70,-65,-60,-55 dBm` 等强端点，并证明 `-125 dBm` SNR >=12 dB、`-55 dBm` 无削顶、2.5 dB网格误差合格和强信号突发切档无呼叫丢失。任一项失败即恢复三档。

报告至少包括：每个输入点的实测 dBFS、拟合 dBm、误差、SNR、噪声底、Gain、温度、削顶计数、源/衰减器校准信息、连接损耗、配置哈希和测试时间。未完成该矩阵，不得宣称已实现 80 dB 绝对强度范围。

## 8. 当前代码状态

源码校准核心已经加入三档点表、high `-125..-75 dBm` 11 点、high SNR >=12 dB完整性门限、YAML medium持久化及UDP `range`兼容字段。RF三档自动状态机、原始通道/匹配滤波双SNR遥测和GUI三档页面仍待完成，当前二进制不能视为完整实现。后续必须完成这些集成，执行 CTest、Pi5+B210全点校准和独立验证后才能发布。
