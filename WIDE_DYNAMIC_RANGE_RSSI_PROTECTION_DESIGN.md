<!--
Compiled and written by BG1KK.
Privatization and closed-source use are strictly forbidden.
GNU Radio components are copyrighted by their respective developers.
All other code copyright © BG1KK.
This copyright statement must be retained.
-->
# B210宽动态RSSI测量与强信号保护设计报告

报告日期：2026-08-16

## 1. 设计目标

本方案解决两个相互冲突的问题：

1. 对正常的`-110 dBm`至`-80 dBm`弱信号尽量准确地测量B210输入端绝对功率。
2. 当2至5米内突然出现47 dBm FM或45 dBm DMR发射信号时，B210不得损坏，接收链
   不得持续过载，并应在量程稳定后恢复解调。

目标输入范围按`-110 dBm`至`+30 dBm`设计，总跨度140 dB。`+30 dBm`不是预计的正常
接收值，而是包含天线增益、反射和估算误差后的保护设计上限。单独依靠AD9361的12位ADC、
片内硬件AGC或软件AGC不能同时覆盖该范围。

## 2. 最坏场强估算

采用自由空间路径损耗、收发天线均为0 dBi的初步估算：

| 信号 | 距离 | 自由空间损耗 | 预计接收功率 |
| --- | ---: | ---: | ---: |
| 145.4 MHz、47 dBm | 2 m | 21.7 dB | +25.3 dBm |
| 145.4 MHz、47 dBm | 5 m | 29.7 dB | +17.3 dBm |
| 438.5 MHz、45 dBm | 2 m | 31.3 dB | +13.7 dBm |
| 438.5 MHz、45 dBm | 5 m | 39.3 dB | +5.7 dBm |

Ettus规定B210各RX端口最大输入为`-15 dBm`。因此VHF最坏估算可能超过端口上限约40 dB。
实际近距离传播受天线方向、极化、人体、反射和馈线耦合影响，不能把自由空间结果当作安全
衰减。射频前端必须按至少`+30 dBm`连续输入能力设计，并留出器件容差。

## 3. 总体方案

推荐采用外部三量程接收前端，每个物理RX通道各安装一套：

```text
天线/双工器
  -> 功率型带通预选器
  -> 20 dB监测耦合端 -> 80 dB对数检波器 -> 比较器/CPLD + 测量ADC
  -> 高功率限幅保护
  -> 故障默认最大衰减的三量程衰减组件
       R0: 约0 dB
       R1: 约45 dB
       R2: 约95 dB
  -> B210 RX端口
  -> AD9361固定手动增益
  -> 原始IQ功率测量
  -> 软件数字AGC
  -> DMR/FM解调
```

核心原则：

- **保护回路不依赖Pi5、Linux、UHD或转发程序。**
- 外部硬件先完成保护和粗量程选择，B210只处理被限制后的约60至70 dB输入范围。
- AD9361正常工作和校准均采用固定手动增益，绝对RSSI不再依赖不可读的片内AGC实时状态。
- 项目软件AGC保留，只负责把IQ幅度调整到解调器合适范围，不参与绝对RSSI计算。
- 强信号同时由独立对数检波器测量，B210量程仅作为交叉校验。

## 4. 外部射频前端要求

### 4.1 带通预选器

- VHF和UHF接收通道使用各自独立的高线性带通、腔体或螺旋滤波器。
- 连续输入功率能力不低于`+33 dBm`。
- 对本机发射频率、谐波和另一频段应提供尽可能高的隔离度。
- 插入损耗、频率响应和温漂作为完整接收链的一部分校准。

预选器只能抑制带外强信号。需要接收的同频强信号不能依靠滤波器保护，必须进入衰减量程。

### 4.2 对数功率检测支路

建议使用20 dB定向耦合或经过标定的宽带取样器，使天线端`-50 dBm`至`+30 dBm`映射为
检测器端`-70 dBm`至`+10 dBm`。检测器应满足：

- 覆盖136 MHz至520 MHz。
- 可用动态范围不低于80 dB。
- 上升响应不慢于100 ns。
- 输出同时送高速比较器和独立ADC。
- 比较器直接覆盖软件控制，强制选择最大衰减R2。

ADL5513是可行的参考器件：官方范围1 MHz至4 GHz、典型动态范围80 dB、响应约20 ns。
具体电路仍需按VHF/UHF频率、耦合度、输入上限和温度重新验证，不能直接使用典型值代替校准。

### 4.3 三量程衰减组件

推荐标称状态如下，实际插入损耗以逐机校准为准：

| 状态 | 标称外部衰减 | 主要天线输入范围 | 对应B210稳态输入 |
| --- | ---: | ---: | ---: |
| R0弱信号 | 0 dB | -115至-75 dBm | -115至-75 dBm |
| R1中等信号 | 45 dB | -85至-25 dBm | -130至-70 dBm |
| R2强信号 | 95 dB | -35至+30 dBm | -130至-65 dBm |

三种量程存在10 dB以上重叠区。量程切换后的B210稳态最大输入约`-65 dBm`，远低于
`-15 dBm`端口上限，也为固定模拟增益和ADC峰值留出余量。

衰减组件可由功率型PIN开关和固定衰减器组成，也可使用多级数字步进衰减器。器件必须满足：

- 第一级连续输入能力至少`+33 dBm`，不能仅依据P1dB典型值选型。
- 最大衰减不少于90 dB。
- 量程切换时间不大于1 us。
- 断电、控制线开路、CPLD复位和Pi5停止时均默认R2最大衰减。
- 各状态回读必须来自硬件锁存或状态引脚，不能只回显软件命令。

HMC624A一类器件可提供31.5 dB、0.5 dB步进，但官方高RF输入额定值为28 dBm，不能直接
作为本方案第一级承受`+30 dBm`最坏输入。第一级必须选更高功率器件或在其前增加经过验证的
功率保护；后级因前级已衰减，功率要求可以降低。

### 4.4 瞬态限幅

量程从R0切到R2之前仍存在纳秒至微秒的传播延迟。必须在B210前增加限幅网络，并通过实测
确认突然施加`+30 dBm`时，B210端口的瞬态峰值始终低于`-15 dBm`。不能仅根据限幅器的
启动时间或“最大输入功率”推断安全，必须测量其flat leakage、脉冲宽度和后级实际峰值。

如果单级限幅器不能达到`-15 dBm`以下，可采用输入限幅、快速强制衰减和B210前二次限幅
组合。该保护指标必须在接入B210前使用功率检测器或示波器完成验收。

## 5. 增益与量程控制策略

### 5.1 AD9361工作方式

为保证绝对RSSI可追溯，建议修改现有契约：

- 正常运行关闭AD9361硬件AGC。
- RX1/RX2分别使用经过校准的固定聚合增益`Gfixed`。
- `Gfixed`不得直接指定为某个经验值。应在R0/R1/R2前端完成后，以B210端口`-65 dBm`
  输入时原始IQ峰值不高于`-8 dBFS`为上限，再确认`-110 dBm`信号具有足够SNR。
- 如果一个固定增益不能同时满足两端，优先增加R1/R2衰减或调整切换阈值，不重新启用
  不可准确遥测的片内AGC。

软件数字AGC继续放在RSSI测量点之后，保持当前DMR/FM解调幅度控制功能。

### 5.2 双时间尺度控制

保护和测量不得使用同一个慢速200 ms平均值：

1. **硬件保护环**：对数检测器、比较器和CPLD在1 us内强制R2。
2. **快速数字过载环**：每100 us至1 ms检查IQ峰值、连续满量程样本和ADC削顶特征。
3. **量程控制环**：使用10至20 ms功率估计和迟滞切换R0/R1/R2。
4. **显示计量环**：使用200 ms去异常平均值更新GUI RSSI。

### 5.3 建议阈值

初始阈值如下，最终值必须由硬件扫频测试确定：

| 动作 | 上升阈值 | 返回阈值 | 最短保持 |
| --- | ---: | ---: | ---: |
| R0切R1 | -75 dBm | -85 dBm | 50 ms |
| R1切R2 | -25 dBm | -35 dBm | 100 ms |
| 硬件紧急R2 | -20 dBm或更高 | 低于-35 dBm | 500 ms |

对数检测器能够覆盖约`-50 dBm`以上的输入，可对突发中强信号提前切换。R0到R1的弱信号
阈值由B210原始IQ判断。任何IQ削顶、检波器超量程、控制通讯故障或状态不一致均立即进入R2。

上升时必须先增加外部衰减，再更新测量量程；下降时必须确认输入稳定并经过保持时间后才
减少衰减。切换后的5至20 ms标记RSSI无效，不使用过渡样本拟合或显示。

## 6. 绝对dBm计算

### 6.1 B210弱/中信号路径

绝对RSSI只能使用软件AGC之前的原始复数IQ：

```text
Ptotal = mean(I^2 + Q^2)
Psignal = max(Ptotal - Pnoise, epsilon)
Pdbfs = 10 * log10(Psignal)
Pin_dBm = Calibration[RX, frequency, range, Gfixed, bandwidth, temperature](Pdbfs)
```

噪声功率必须在线性域相减，不能直接进行dB相减。`Pnoise`可由相邻空闲信道、通话前噪声
窗口或受控静噪窗口估计。计算窗口应排除量程切换、ADC削顶、UHD溢出和静噪填零样本。

拟合使用单调分段线性插值或单调三次插值，不使用高阶全局多项式，避免端点振荡。每个输出
同时携带量程、校准版本、有效状态和估计误差。

### 6.2 独立强信号路径

对数检测器经过自身温度和频率校准后，直接计算天线参考面`Pdet_dBm`。建议使用范围：

- `-110 dBm`至约`-55 dBm`：主要采用B210校准值。
- `-55 dBm`至约`-40 dBm`：B210与检测器重叠，进行一致性检查和加权融合。
- 高于约`-40 dBm`：主要采用检测器值，B210 R1/R2结果用于交叉检查。

当两路在重叠区相差超过3 dB，应标记校准或硬件故障，不应静默选择其中一路。

## 7. 校准数据结构

每个校准数据集至少包含：

- 物理RX编号和实际天线端口。
- 中心频率或信道编号。
- VHF/UHF预选器和外部量程R0/R1/R2。
- 外部量程实际衰减、AD9361固定增益和UHD版本。
- 接收带宽、采样率和数字滤波器版本。
- B210温度、外部前端温度、校准时间和仪表信息。
- 每个输入dBm对应的原始dBFS、标准差、SNR、削顶计数和拟合残差。
- 对数检测器ADC码、检测电压和对应输入dBm。

当前仅按RX1/RX2、高/低档保存四列数据不足以支持本方案。配置结构应升级为
`RX × frequency/channel × external_range × fixed_gain × temperature_band`，并保留旧数据迁移版本。

### 建议校准点

| 数据集 | 输入范围 | 步进 |
| --- | ---: | ---: |
| R0 | -115至-65 dBm | 5 dB |
| R1 | -85至-15 dBm | 5 dB |
| R2 | -35至+30 dBm | 5 dB |
| 对数检测器 | -50至+30 dBm | 5 dB |

每个状态另用位于校准点之间的独立2.5 dB或3 dB点验收，不能用参与拟合的数据评价精度。
校准参考面必须明确为B210外部前端的天线输入端；使用信号源、功率计、定值衰减器和电缆
损耗建立可追溯输入。`+20 dBm`以上校准必须使用额定功率足够的耦合器和衰减器，禁止将
大功率发射机直接连接到B210或普通信号源端口。

## 8. 目标精度

以下作为工程验收目标，不是未经测试即可保证的指标：

| 输入范围 | 绝对误差目标 | 重复性目标 |
| --- | ---: | ---: |
| -110至-80 dBm | 不大于±2 dB | 不大于1 dB |
| -80至-40 dBm | 不大于±2 dB | 不大于1 dB |
| -40至+20 dBm | 不大于±2.5 dB | 不大于1 dB |
| +20至+30 dBm | 不大于±3 dB | 不大于1.5 dB |

温度超出已校准范围、频率无对应数据、量程状态不可信或检测器饱和时，GUI必须显示“未校准”
或“超量程”，不能继续显示看似精确的dBm数值。

## 9. 强信号安全状态机

```text
上电/复位/看门狗超时 -> R2最大衰减

R0:
  输入 > -75 dBm -> R1
  检测器紧急或IQ削顶 -> R2

R1:
  输入 > -25 dBm -> R2
  输入 < -85 dBm并保持 -> R0
  检测器紧急或状态异常 -> R2

R2:
  输入 < -35 dBm并保持 -> R1
  检测器超量程 -> 保持R2并报告OVER
```

量程切换不能停止UHD流图。只丢弃过渡窗口的RSSI样本，DMR/FM解调在软件AGC重新稳定后
继续。必要时允许损失强信号刚出现时的少量符号，但不得以保持首帧为理由降低硬件保护等级。

## 10. GUI与通讯要求

新增状态字段：

- `external_range`：R0/R1/R2。
- `external_attenuation_db`：硬件回读的实际状态对应值。
- `rf_detector_dbm`及`rf_detector_valid`。
- `b210_raw_dbfs`、`b210_clipped_samples`。
- `rssi_dbm`、`rssi_source`、`rssi_uncertainty_db`和`rssi_valid`。
- `rf_protection_active`、`rf_overrange`和`range_switch_count`。

GUI状态页同时显示量程、外部衰减、固定AD9361增益、检测器功率和最终RSSI。保护动作使用
明确的红色“RF保护”状态；校准失效时回退显示dBFS，不显示伪dBm。

## 11. 验收方法

### 静态精度

1. 在145.4 MHz和438.5 MHz分别完成全量程扫点。
2. 每个校准点稳定采集不少于5秒。
3. 使用独立中间点验证误差、重复性和单调性。
4. 在RX1/RX2、R0/R1/R2和规定温度范围内分别验收。

### 动态保护

1. 使用隔离良好的射频阶跃装置，从`-100 dBm`直接切换到`+30 dBm`。
2. 在不连接B210时先测量保护前端输出，确认任何瞬态均低于`-15 dBm`。
3. 验证硬件强制R2时间不大于1 us，软件状态上报不大于20 ms。
4. 连续施加`+30 dBm`不少于60秒，器件不得损坏、热失控或退出保护。
5. 强信号撤销后按迟滞返回，不得在量程间振荡。

### 接收业务

1. 在`-110 dBm`至`-80 dBm`范围验证FM、CTCSS、DMR同步和音频。
2. 在R0/R1及R1/R2重叠区验证切换前后RSSI连续性。
3. 突发强FM/DMR信号后验证接收链恢复、无持续ADC削顶和无UHD溢出。
4. 使用带外大信号验证预选器隔离和接收阻塞性能。

## 12. 实施顺序

1. 先制作无B210连接的外部保护样机，完成`+30 dBm`阶跃安全验收。
2. 增加CPLD/微控制器、检测器ADC和Pi5通讯，验证断电默认R2和硬件回读。
3. 将转发程序改为固定AD9361增益，接入量程状态和原始IQ计量。
4. 扩展配置、UDP协议和GUI，再完成多维校准数据迁移。
5. 完成实验室静态、动态和温度测试后，才允许接入47/45 dBm实际发射设备测试。

## 13. 结论

本项目需要覆盖约140 dB天线输入范围，而B210只能在某一时刻处理其中一段。可行方法不是
继续扩大软件增益，而是通过外部0/45/95 dB三量程把任意稳态输入映射到B210约65 dB窗口，
再用固定AD9361增益和每量程端到端校准计算dBm。独立80 dB对数检测器负责强信号测量，
硬件比较器和故障默认最大衰减负责突然大信号保护。

该方案要求修改“正常运行启用AD9361硬件AGC”的现有契约。若继续使用片内AGC，仍然必须
增加同样的外部保护，而且绝对RSSI还需要改UHD驱动读取实时增益索引，复杂度和误差都更高。

## 14. 原厂资料

- Ettus B200/B210官方硬件说明：<https://files.ettus.com/manual/page_usrp_b200.html>
- Analog Devices AD9361 Reference Manual UG-570：
  <https://www.analog.com/media/en/technical-documentation/user-guides/AD9361_Reference_Manual_UG-570.pdf>
- Analog Devices ADL5513官方产品资料：<https://www.analog.com/en/products/adl5513.html>
- Analog Devices HMC624A数据手册：
  <https://www.analog.com/media/en/technical-documentation/data-sheets/hmc624a.pdf>
